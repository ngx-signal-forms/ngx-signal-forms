# Fieldset Grouping + Errors (archived reference)

This dedicated grouping page was merged into `form-field-wrapper/complex-forms` to keep grouped sections, nested arrays, and wrapper behavior in one realistic example.

## What still matters from this folder

- `NgxSignalFormFieldset` usage
- aggregated section errors
- grouped radio/fieldset placement behavior

## Prefer this live example instead

- `form-field-wrapper/complex-forms`

The source files remain useful if you want an isolated implementation reference for grouped summaries outside the larger long-form demo.
