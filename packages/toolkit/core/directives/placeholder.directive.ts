import { Directive } from '@angular/core';

/**
 * Placeholder directive for the toolkit.
 * This will be replaced with actual directives (auto-aria, auto-touch, etc.) in future iterations.
 *
 * @example
 * ```html
 * <input [control]="form.email" />
 * ```
 */
@Directive({
  selector: '[ngxSignalFormPlaceholder]',
  standalone: true,
})
export class NgxSignalFormPlaceholderDirective {
  constructor() {
    // Placeholder directive - will be replaced with actual implementation
  }
}
