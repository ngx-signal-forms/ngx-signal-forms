name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version bump type or exact version'
        required: true
        type: choice
        options:
          - patch
          - minor
          - major
          - prepatch
          - preminor
          - premajor
          - prerelease
          - custom
      custom_version:
        description: 'Custom version (only if "custom" is selected)'
        required: false
        type: string
      prerelease_id:
        description: 'Prerelease identifier (for pre* versions)'
        required: false
        type: choice
        default: 'beta'
        options:
          - beta
          - next
          - rc
          - alpha
      dry_run:
        description: 'Dry run (no actual publish)'
        required: false
        type: boolean
        default: false
      first_release:
        description: 'First release (no previous git tags exist)'
        required: false
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          filter: tree:0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: pnpm/action-setup@v4
        name: Install pnpm
        with:
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Nx Report
        run: pnpm exec nx report

      - name: Verify Quality (Lint, Test, Build, E2E)
        run: pnpm exec nx run-many -t lint test build e2e-ci -p toolkit demo-e2e

      - name: Determine version command
        id: version_cmd
        run: |
          VERSION="${{ inputs.version }}"
          if [ "$VERSION" = "custom" ]; then
            echo "cmd=${{ inputs.custom_version }}" >> $GITHUB_OUTPUT
          elif [[ "$VERSION" == pre* ]]; then
            # prepatch, preminor, premajor, prerelease all use --preid
            echo "cmd=$VERSION --preid=${{ inputs.prerelease_id }}" >> $GITHUB_OUTPUT
          else
            echo "cmd=$VERSION" >> $GITHUB_OUTPUT
          fi

      - name: Set release flags
        id: release_flags
        run: |
          FLAGS=""
          if [ "${{ inputs.first_release }}" = "true" ]; then
            FLAGS="$FLAGS --first-release"
          fi
          echo "flags=$FLAGS" >> $GITHUB_OUTPUT

      - name: Version (Dry Run)
        if: ${{ inputs.dry_run }}
        run: |
          pnpm exec nx release version ${{ steps.version_cmd.outputs.cmd }} --dry-run ${{ steps.release_flags.outputs.flags }}
          pnpm exec nx release changelog --dry-run ${{ steps.release_flags.outputs.flags }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Version (Real)
        if: ${{ !inputs.dry_run }}
        run: |
          pnpm exec nx release version ${{ steps.version_cmd.outputs.cmd }} --yes ${{ steps.release_flags.outputs.flags }}
          pnpm exec nx release changelog --yes ${{ steps.release_flags.outputs.flags }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Summary
        if: ${{ !inputs.dry_run }}
        run: |
          echo "## Release Summary" >> $GITHUB_STEP_SUMMARY
          echo "- Version: ${{ steps.version_cmd.outputs.cmd }}" >> $GITHUB_STEP_SUMMARY
          echo "- Dry Run: ${{ inputs.dry_run }}" >> $GITHUB_STEP_SUMMARY
