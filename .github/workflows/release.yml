name: Release

# Manually create versioned releases with changelog and git tags
# This workflow does NOT publish to npm - use "Publish" workflow after this succeeds
#
# Release types:
#   - patch/minor/major: Stable releases → npm tag: latest
#   - prerelease --preid=beta: Beta releases → npm tag: beta (e.g., 1.0.0-beta.1)
#   - prerelease --preid=rc: Release candidates → npm tag: rc (e.g., 1.0.0-rc.1)
#   - For @next dev builds, use "Release Next" workflow instead
on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version bump type or exact version'
        required: true
        type: choice
        options:
          - patch
          - minor
          - major
          - prepatch
          - preminor
          - premajor
          - prerelease
          - custom
      custom_version:
        description: 'Custom version (only if "custom" is selected)'
        required: false
        type: string
      prerelease_id:
        description: 'Prerelease identifier (for pre* versions)'
        required: false
        type: choice
        default: 'beta'
        options:
          - beta
          - rc
          - alpha
      dry_run:
        description: 'Dry run (no actual publish)'
        required: false
        type: boolean
        default: false
      first_release:
        description: 'First release (no previous git tags exist)'
        required: false
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          filter: tree:0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: pnpm/action-setup@v4
        name: Install pnpm
        with:
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Nx Report
        run: pnpm exec nx report

      - name: Verify Quality (Lint, Test, Build, E2E)
        run: pnpm exec nx run-many -t lint test build e2e-ci -p toolkit demo-e2e

      - name: Determine version command
        id: version_cmd
        run: |
          VERSION="${{ inputs.version }}"
          if [ "$VERSION" = "custom" ]; then
            echo "cmd=${{ inputs.custom_version }}" >> $GITHUB_OUTPUT
          elif [[ "$VERSION" == pre* ]]; then
            # prepatch, preminor, premajor, prerelease all use --preid
            echo "cmd=$VERSION --preid=${{ inputs.prerelease_id }}" >> $GITHUB_OUTPUT
          else
            echo "cmd=$VERSION" >> $GITHUB_OUTPUT
          fi

      - name: Set release flags
        id: release_flags
        run: |
          FLAGS=""
          if [ "${{ inputs.first_release }}" = "true" ]; then
            FLAGS="$FLAGS --first-release"
          fi
          echo "flags=$FLAGS" >> $GITHUB_OUTPUT

      - name: Release (Dry Run)
        if: ${{ inputs.dry_run }}
        run: pnpm exec nx release ${{ steps.version_cmd.outputs.cmd }} --skip-publish --dry-run ${{ steps.release_flags.outputs.flags }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Release
        if: ${{ !inputs.dry_run }}
        run: pnpm exec nx release ${{ steps.version_cmd.outputs.cmd }} --skip-publish ${{ steps.release_flags.outputs.flags }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Push changes and tags
        if: ${{ !inputs.dry_run }}
        run: git push --follow-tags

      - name: Summary
        run: |
          echo "## Release Summary" >> $GITHUB_STEP_SUMMARY
          echo "- Version: ${{ steps.version_cmd.outputs.cmd }}" >> $GITHUB_STEP_SUMMARY
          echo "- Dry Run: ${{ inputs.dry_run }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ inputs.dry_run }}" != "true" ]; then
            echo "### Next Steps" >> $GITHUB_STEP_SUMMARY
            echo "The **Publish** workflow will automatically trigger to publish to npm." >> $GITHUB_STEP_SUMMARY
            echo "Or manually run it from Actions → Publish → Run workflow" >> $GITHUB_STEP_SUMMARY
          fi
